{{#each users}}
<div class="card">
    <div class="card-content">
        <a class="card-title" href="/users/{{id}}">
        {{name}}
        {{events}}
        {{image}}
        </a>
    </div>
</div>
{{/each}}